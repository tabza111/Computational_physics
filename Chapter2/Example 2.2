# Example: 2.2
from math import sin, cos, pi
r=float(input("Enter r:"))
d=float(input("Enter theta in degrees:"))

theta=d*pi/180  # from degree to radian
x=r*cos(theta)
y=r*sin(theta)

print("x=", x, "y=", y)
